# 数据整理与近似去重候选实验

## 实验目标

构建一个可复现的小型数据整理实验，验证以下流程：

1. 文本规范化与基础质量过滤。
2. 保持顺序的精确去重。
3. 基于 token 集合 Jaccard 相似度生成近似重复候选。
4. 使用人工金标准评估候选召回率、候选精确率和候选缩减率。
5. 在最低候选召回率约束下选择相似度阈值。

候选生成只缩小需要复核的文本对范围，不直接执行删除。

## 可复现运行

```powershell
python data_curation_experiment.py
```

脚本内置断言覆盖数据清洗、精确去重、近似重复候选、人工标签完整性、指标计算、阈值敏感性、约束选择和失败案例定位。

## 金标准

实验包含 5 条文本，共形成 10 个无序文本对。人工标注确认以下 2 对为真实重复：

- `(0, 1)`：两段文本都表达模型根据上下文预测下一个 token。
- `(3, 4)`：两段文本都表达 Tokenizer 把文本转换为 token ID。

其余 8 对标为不重复。金标准覆盖候选生成前的全部 10 个文本对，因此能够识别未进入候选集合的真实重复对。

## 评测指标

设 `U` 为全部无序文本对，`C` 为候选文本对，`G` 为人工标注的真实重复文本对。

- 候选召回率：`|C ∩ G| / |G|`。
- 候选精确率：`|C ∩ G| / |C|`。
- 候选缩减率：`1 - |C| / |U|`。

当金标准中没有真实重复对时，候选召回率未定义。当候选集合为空时，候选精确率未定义。

## 阈值敏感性结果

| Jaccard 阈值 | 候选对数 | 候选召回率 | 候选精确率 | 候选缩减率 |
|---:|---:|---:|---:|---:|
| 0.4 | 3 | 100% | 66.7% | 70% |
| 0.5 | 2 | 50% | 50% | 80% |
| 0.6 | 1 | 0% | 0% | 90% |
| 0.7 | 0 | 0% | 未定义 | 100% |

在候选召回率必须达到 100% 的实验约束下，选择阈值 `0.4`。该阈值保留 3 个候选对，找回全部 2 个真实重复对，并减少 70% 的后续比较量。

## 倒排索引预筛结果

原始两两比较需要计算 10 次 Jaccard。未过滤高频 token 的倒排索引仍产生全部 10 个文本对，因为 `token` 出现在全部 5 条文本中。

设置最大文档频率为 3 后，倒排索引产生 4 个预候选对，将实际 Jaccard 计算量减少 60%。两个真实重复对都保留在预候选中，因此该小样本上的预筛召回率为 100%。经过 Jaccard 阈值 `0.4` 后，最终仍得到原来的 3 个候选对。

在当前 5 条文本上，最大文档频率计数 `3` 等价于最大文档频率比例 `0.6`。比例阈值能在数据规模变化时保持相对含义，但仍需在有人工金标准的调参集上选择，并在独立测试集上验证。

预筛召回率与最终候选召回率必须分别报告。真实重复对一旦被倒排索引预筛排除，后续相似度计算无法恢复该文本对。

## 失败案例

### 假阴性

- 文本对：`(0, 1)`。
- Jaccard 相似度：约 `0.444`。
- 人工标签：重复。
- 候选结果：阈值 `0.5` 时未进入候选集合。
- 原因：两段文本使用不同表面表达，token 集合重叠不足，但核心语义相同。
- 风险：真实重复数据残留，候选召回率下降。

### 假阳性

- 文本对：`(0, 2)`。
- Jaccard 相似度：`0.625`。
- 人工标签：不重复。
- 候选结果：阈值 `0.5` 时进入候选集合。
- 原因：大量普通 token 重叠掩盖了 `next`、`previous` 和 `future` 所表达的关键语义差异。
- 风险：若把候选结果直接用于自动删除，可能误删具有不同含义的有效文本。

## 结论与边界

本实验说明 token Jaccard 可以作为便宜、可解释的候选生成信号，但不能作为最终重复判断。候选阶段应优先控制真实重复对漏检，再通过人工复核或更强的语义方法处理假阳性。

当前结果只来自 5 条文本和 10 个文本对，并且同一金标准被用于阈值选择和效果观察，因此不能代表真实预训练语料上的泛化效果。下一步应按来源文档或重复内容组切分调参集与独立测试集，扩大来源和语义类型覆盖，再报告独立测试结果。
