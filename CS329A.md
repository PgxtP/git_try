# 课程导论与概览
1. 计算量增大，loss下降
2. 数据量增大，loss下降
3. 参数数量增大，loss下降

模型规模不断增大

模型大，各种测试分数上升，学会了少样本学习，推理，少样本或零样本都需要大模型才能实现

思维链很重要，模型从公开的思维链受益

指令微调和基于人类反馈的强化学习也很重要，是gpt成功的关键
## Finetuning the Model to Human Preferences! & From Test Time Scaling to Distilling Synthetic Reasoning Traces
### (Pre-)training(all)
最简单的，只需要训练这个模型就行了
### Fine-tuning(all)
 1. During Finetuning LLMs Are Aligned with Human Preferences

    针对预训练后的模型进行微调
    训练中不断调整，让模型得出的更贴近人类想要的(人类的偏好和价值观)

    使用精选的数据(很贵)
2. Instruction tuning:Fine-tuning on Many Tasks

    指令 问题 答案
    模型在这个环节里学习如何理解问题，针对这些问题给出相应的答案
    这个过程就是，指令 问题 答案的配对
指令微调后，模型具备了理解问题，一步步走完流程，得出答案的能力
3. Finetuning with Human Preferences(RLHF)

    利用人类的偏好，继续微调(基于人类偏好的强化学习)

    从人类的偏好构建出一个奖励模型，花很多钱请专家或者普通人

    通过奖励模型引导大语言模型的参数
真正的核心部分还是pre-training fine-tuning higher quality data instruction RLHF

但是2.5年之前，推理也成了提升模型能力的一个前沿方向，开辟了大量全新的研究方向和路径

### Inference（**很重要**）(front)
一遍又一遍地要求模型去解决同一个给定的输入问题

模型回答很多次，假设有验证器或选择机制，挑出正确回答作为正确答案

此处用的是重复采样

不碰任何参数，模型本身固定，只是推理过程中生成更多多样性答案，解决对模型来说更难的问题

### Test-time(behind)
把微调和测试时扩展两项技术结合到一起，可以用测试时产生的优质数据用来微调，这个成为了思考模型和推理模型的重要组成部分(很重要，没有边界，值得探索)是斯坦福教授特别看重的自我改进的部分

## Reasoning Models
测试时拓展，思考模型使用思维链等，任务分析，任务分解，自我评估策略（跑一下看结果对不对并自我修正），某个方法行不通模型可以回溯，尝试另一种思路

其实目的是让模型知道怎么对，怎么好

基础模型一开始确实具备思考能力，但需要一定程度的思考，不过也是需要微调(引导)