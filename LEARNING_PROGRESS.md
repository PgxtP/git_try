# 学习进度与薄弱点

更新时间：2026-09-23。

## 当前已通过

- 能区分 `Dataset` 负责单样本、`DataLoader` 负责批处理。
- 能解释训练更新参数、验证只评估模型。
- 能解释 `model.train()`、`model.eval()` 与 `torch.no_grad()` 的不同职责。
- 能写出 padding mask 与 causal mask 的用途。
- 能判断编码器自注意力使用源 padding mask、解码器自注意力使用目标 padding mask 与 causal mask、交叉注意力使用源 padding mask。
- 能解释 teacher forcing 与自回归生成的输入来源差异。
- 能解释训练阶段可并行处理目标序列、推理阶段必须自回归生成的原因。
- 能用“查询、匹配特征、实际汇总内容”解释 Q、K、V。
- 能说明自注意力的 Q、K、V 虽来自同一输入，但因三个线性投影层的参数独立，投影结果通常不同。
- 能说明交叉注意力的 Q 来自解码器当前隐藏状态，K、V 来自编码器 `memory`。
- 能写出缩放点积注意力的计算顺序，并解释 mask 必须位于 softmax 之前。
- 能无提示写出 `zero_grad → loss → backward → step` 的梯度更新顺序。
- 能从 `TranslationDataset` 类创建数据集实例，并把实例传给 `DataLoader`。
- 能从完整目标序列切出 teacher forcing 的解码器输入与监督标签，并判断切片后的 shape。
- 能调用 `model(src_tokens, decoder_input)` 完成前向传播，并判断 logits 的 `[B, T, V]` shape。
- 能无提示组合 teacher forcing、模型前向传播、交叉熵、反向传播与优化器更新，完成一个训练 batch。
- 能解释多头注意力从 `[B, L, D]` 拆分为 `[B, H, L, d_head]`、形成分数矩阵并合并回 `[B, L, D]` 的 shape 变化。
- 能写出多头注意力的拆头与合并代码。
- 能按“投影拆头 → 缩放点积注意力 → 合并多头 → 输出投影 → 返回结果”的顺序说明 `MultiHeadAttention.forward`。
- 能无提示写出编码器层的自注意力、残差连接、LayerNorm 和前馈子层。
- 能分别写出解码器的因果自注意力、交叉注意力和前馈子层，并正确连接残差与 LayerNorm。
- 能无提示一次性组合完整的 `DecoderLayer.forward`。
- 能判断验证损失最低的 epoch，并识别之后出现的过拟合。
- 能判断编码器、解码器和注意力中的主要 shape。
- 能在交叉注意力中分别保留查询长度与键值长度，并判断拆头后 Q、K、V 和注意力权重的 shape。
- 能解释合并多头前必须交换头维与序列维，避免把同一头的不同 token 错误拼接。
- 能说明多头合并后的 shape 已经正确，并解释 `out_proj` 用于可学习地重新混合各头特征。
- 能识别批量贪心解码只检查当前步 `EOS` 的缺陷：不同样本若在不同时间结束，循环可能无法及时停止。
- 能说明 `EOS` 本身不会让 Transformer 停止计算，batch 解码时需要显式记录已结束样本并保持其后续 token 为 `EOS`。
- 项目已为批量贪心解码加入累计 `finished` 状态，并添加不同样本异步结束的回归检查。
- 能判断 `finished.unsqueeze(1)` 会把 `[B]` 变为 `[B, 1]`，从而与批量 `next_token` 逐行对齐。
- 能把完整文本序列错位构造成 next-token prediction 的模型输入与监督标签，并正确保留末尾 `EOS`。
- 能解释 next-token prediction 属于自监督学习：监督信号由原始文本自身自动产生，不需要人工逐条标注。
- 能解释普通监督学习使用预先提供的输入与目标标签配对，并以商品评论分类为例指出模型学习从输入评论到情感类别的映射。
- 能区分典型 LLM 预训练与普通监督学习：两者都使用目标标签、损失函数和反向传播，但 next-token 预训练的目标由原始文本自动错位产生，普通监督学习的任务标签作为额外监督信息提供。
- 能说明在总计算预算固定时，单独增大模型会挤压可训练 token 数并可能造成训练不足，因此不能仅凭参数量增大断定验证损失一定下降。
- 能解释计算最优分配是在固定计算预算下平衡参数量与训练 token 数，并应在相同验证集和评测条件下比较验证损失，而不能按参数量大小直接判定方案优劣。
- 能说明 Scaling Laws 是在特定模型、Tokenizer、数据分布和训练方法下拟合出的经验规律；条件变化时，拟合参数与损失可比性可能改变，不能把旧曲线当作数学保证直接外推。
- 能根据实验控制条件限定 Scaling Laws 结论：保持训练 token 数而增大模型会同时增加计算量，因此该实验不能证明固定计算预算下更大的模型更优。
- 能限定训练步数实验的结论范围：固定模型和数据而改变训练步数只能研究训练计算量，不能据此判断更大的模型是否更好。
- 能根据训练步数实验解释计算量与数据量需要匹配：固定小语料上持续增加训练计算会产生过拟合，需要同步增加有代表性的高质量数据。
- 能区分名义训练 token 数与有效数据多样性：重复复制同一语料会增加 token 计数，但不会等比例增加独立信息，并可能放大过拟合和分布偏差。
- 能说明数据处理 A/B 实验必须保持原始数据切分、Tokenizer、模型、随机种子、训练步数和验证集等条件一致，否则无法把 loss 差异归因于数据处理方法。
- 能说明 `build_next_token_examples` 的两个输入分别是有序 token ID 序列和单个输入窗口长度，并理解长度恰好等于窗口时因为缺少右移后的下一个真实 token 而不能构造完整训练样本。
- 已独立写出多窗口 next-token 样本的完整输入与右移标签，滑动窗口核心逻辑复测通过。
- 能说明 padding 只用于对齐样本 shape，不能冒充原始文本中的真实 next-token 标签；若 padding 参与损失，模型会被错误训练为预测 padding。
- 能说明单个随机种子的结果可能受初始化和数据顺序影响，不能据此声称数据处理方法能稳定降低验证 loss，需要使用相同的一组种子重复两种方案并汇总结果。
- 能说明多随机种子比较应让两种方案使用相同种子形成配对，否则初始化和数据顺序的差异会与数据处理方法混杂。
- 能区分多种子胜率与效果大小：胜率只表示方向一致性，仍需结合配对平均差值和差值标准差判断改善幅度及其稳定性。
- 能说明 context length 限制单次计算的可见 token 窗口，而不是限制整个训练语料，并理解窗口会随语料切分或滑动。
- 能说明 context length 按 Tokenizer 输出的 token 数计算；一个单词可能被拆成多个 token，特殊 token 也可能占用位置。
- Tokenizer 与 context length 已复测通过：不同 Tokenizer 会使同一原始文本产生不同 token 数，固定窗口可能因此覆盖不同数量的原始内容。
- 能解释子词 Tokenizer 可把未见过的完整单词拆成已知片段，保留部分构词和语义线索，并知道子词不一定是语言学词根。
- 能区分 Tokenizer 与语言模型的职责：Tokenizer 只切分并映射 ID，具体词义需要模型结合上下文学习和判断。
- 能说明相同 token ID 使用相同初始 embedding，但经过自注意力汇总不同上下文后，可以形成不同的隐藏状态。
- 能解释 GPT 类模型进行 next-token prediction 时需要 causal mask，以防模型看到未来 token 而发生标签泄漏。
- 能说明 GPT 使用 decoder-only Transformer，自注意力的 Q、K、V 都来自当前 token 序列，不依赖独立编码器的 `memory`。
- 能区分 pretraining 与 in-context learning：前者通过反向传播更新参数，后者在推理时利用 prompt 中的说明或示例且不更新参数。
- 能根据 prompt 中的示例数量区分 zero-shot、one-shot 与 few-shot in-context learning。
- 能说明 few-shot 示例并非越多越好，需要同时比较准确率、输入 token、延迟以及示例质量。
- 能解释预训练数据重复会降低多样性，并说明训练数据包含评测题或近似答案会造成污染、记忆和泛化能力高估。
- 能区分精确去重与近似去重，并判断大小写、标点或空格差异会使未经预处理的字符串无法精确匹配。
- 能说明文本规范化有助于去重，但过度删除标点或统一大小写可能破坏代码、专有名词和其他有效语义。
- 能区分去重与质量过滤：唯一文本仍可能是乱码、广告或错误信息，不能仅因不重复就进入预训练数据。
- 能说明数据清洗包括编码、空白、控制字符、损坏格式和低质量内容处理，并知道清洗不等于无条件删除标点或统一大小写。
- 已实现并通过空白规范化测试，能使用正则压缩连续空白，并修正方法调用需要括号的机械语法问题。
- 已实现并通过保持原顺序的精确去重测试：使用集合记录已出现字符串，并用列表保留第一次出现的位置。
- 已实现并通过规范化后精确去重测试：先逐项规范化空白，再保持原顺序删除重复文本。
- 已实现并通过去重删除率测试，包含空数据集的除零保护，并知道该指标不能单独代表剩余数据质量。
- 已实现并通过去重策略对照实验测试：在同一输入上比较直接去重与规范化后去重的保留数量和删除率。
- 已实现并通过空文本过滤测试：按空白规范化、空文本过滤、保持原顺序精确去重的顺序整理文本。
- 已实现并通过分阶段清洗指标测试：分别记录原始、非空、唯一数量，以及空文本删除数和重复删除数。
- 已实现并通过完整清洗指标一致性测试，覆盖空文本、重复和长度过滤三个阶段，并能识别被篡改的统计。
- 已实现并通过最小长度过滤测试，并记录其风险：短文本可能有效，长度阈值必须显式记录并结合误删案例评估。
- 已实现并通过长度过滤样本分组测试，能区分误删与漏删，并保留被删除样本供人工抽查。
- 能结合保留率与被删除样本的人工抽查结果选择长度阈值，优先控制有效文本误删风险。
- 能说明多来源数据应采用分层随机抽查，为小来源设置最低抽查数量，并分别报告来源误删率后按真实来源占比加权。
- 项目已实现并通过来源加权误删率测试，避免把过采样后的抽查比例直接当作总体误删率。
- 能说明人工抽查需要统一标注指南、正反例与边界案例，并通过指定复核人处理和记录标注分歧。
- 能说明质量判断不能只看文本，还应保留来源、上下文、任务情景和对话角色等元数据。
- 已理解并通过上下文感知去重验收：使用规范化后的上下文与文本作为组合键，只合并两者都相同的记录。
- 项目已实现并通过来源合并去重测试：重复内容仅保留一份，并把不同来源聚合到 `sources` 列表中。
- 能解释近似文本只能作为候选重复项，高相似度仍可能因关键实体、否定词或类型词不同而具有不同含义，不能直接自动删除。
- 能说明近似度函数应优先返回连续分数，以支持阈值比较、边界案例分析和误删审计。
- 已实现并通过 token 集合 Jaccard 相似度测试，覆盖完全相同、部分重叠、空文本和关键类型词差异案例。
- 能解释 token 集合 Jaccard 的局限：集合会丢失词频和词序，因此分数为 `1.0` 也不代表原文逐字相同。
- 能区分记录级去重与 token 集合转换：前者删除整条重复记录，后者改变单条记录的相似度表示并丢失词频与词序。
- 能解释近似重复阈值的权衡：提高阈值通常减少误报和复核量，但可能增加真实重复文本的漏检。
- 已实现并通过近似重复候选对筛选测试，仅枚举无序索引对，并保留达到阈值的连续 Jaccard 分数。
- 能解释全量两两比较具有平方级计算成本，分桶规则需要在真实重复项漏检与时间、内存开销之间权衡。
- 项目已实现并通过无序两两比较数量测试，用稳定的候选对计数展示规模增长。
- 能联合候选召回率与候选缩减率评价分桶方案，并在重复污染风险较高时优先控制真实重复对漏检。
- 能说明仅知道候选缩减率不能推断候选召回率；计算候选召回率还需要全部真实重复对，以及其中进入候选集合的真实重复对。
- 能说明小型金标准评测集应覆盖候选生成前的全部无序文本对，而不能只标注候选生成器已经返回的文本对。
- 已完成小型人工重复对金标准的10个语义标签，并通过标签结构与完整性检查。
- 能用候选集合与真实重复集合的交集解释候选召回率，并说明假阳性候选不会计入召回率分子。
- 能判断候选召回率属于候选生成的硬约束；召回率未达标时，不能用更高的候选缩减率补偿漏检。
- 已根据候选召回率必须达到 `100%` 的约束选择阈值 `0.4`，对应候选缩减率为 `70%`。
- 能说明不能在同一金标准评测集上同时选择阈值并报告最终泛化效果，否则可能产生调参过拟合与乐观偏差。
- 能说明应先按原始文本、来源文档或重复内容组切分调参集与测试集，再分别生成文本对，避免共享内容造成评测泄漏。
- 能说明 token Jaccard 衡量表面 token 集合重叠而非语义重复关系，因此高分和低分都不能直接作为最终重复判断。
- 能说明候选集合中的假阳性不会降低候选召回率，但会降低候选精确率并增加后续复核工作量。
- 能区分近似重复候选生成与最终删除决策，并说明候选文本对还需要人工复核或更强的语义判断。
- 项目已形成 `DATA_CURATION_REPORT.md`，记录可复现流程、金标准、评测指标、阈值敏感性、失败案例和实验边界。
- 能把当前结果准确表述为小型可复现实验，并说明泛化验证需要来源有代表性、与调参集隔离且带人工金标准的独立测试集。
- 能说明当前全量两两比较会先计算所有文本对的 Jaccard，相似度阈值只减少返回候选与后续复核量，并未减少候选生成阶段的比较次数。
- 已在机械 API 修正后实现 token 倒排索引候选生成：只组合同一倒排列表中的无序文本对，并用集合合并由多个 token 重复产生的候选。
- 能说明低频 token 的倒排列表更短、生成候选对更少，因此通常比出现在全部文本中的高频 token 更有筛选能力。
- 能说明高频 token 过滤不能无条件执行；若真实重复对只共享被过滤的 token，它会在 Jaccard 计算前成为漏检。
- 能说明未进入倒排索引预候选集合的文本对不会再计算 Jaccard；无共享有效 token 的语义重复对可能因此在预筛阶段漏检。
- 能说明候选召回率优先时应使用多个候选生成通道的并集，使任一通道找到的文本对都能进入后续判断。
- 能说明合并多个候选通道后必须重新评测候选召回率、候选精确率、候选缩减率和实际比较成本，并知道候选精确率最直接反映假阳性比例。
- 能说明文档频率比例比固定文档频率计数更容易迁移到不同规模的数据集，因为它保留 token 覆盖范围的相对含义。
- 能区分词频与文档频率，并说明同一 token 在单条文本内重复出现只会使该文本对文档频率贡献一次。
- 能说明通过基础清洗和去重并不代表文本适合训练，最终筛选还必须检查数据与目标任务是否匹配。
- 已实现并通过长度阈值敏感性测试：对同一批文本比较多个阈值下的保留数量，并复用统一过滤函数。
- 已实现并通过完整清洗顺序测试：先规范化、过滤空文本并去重，再执行最小长度过滤。
- 已实现并通过完整分阶段清洗指标测试，分别计算空文本、重复和过短文本的删除数量。
- 已在提示下完成缩放点积注意力、多头注意力、编码器层、解码器层和完整 Transformer 前向传播。

## 仍需重点复测

1. 固定 API 拼写、函数调用括号和二维切片标点仍偶尔出现机械错误。
2. 新函数练习开始前，需要先明确每个输入变量的类型、含义、边界和预期输出，再进入核心逻辑实现。
3. `build_next_token_examples` 首次实现未通过，函数在讲解请求后由助手修正；随后已在明确输入变量后独立通过滑动窗口与错位标签复测。
4. 下一阶段：前两周严格验收已经完成，可以进入第 3 周的后训练与对齐内容。

## 前两周严格审计

- 第 1 周的 Transformer 核心知识、模型实现、shape 说明、训练曲线、验证结果和可复现运行已经完成并通过检查。
- 第 1 周计划指定的 `notes/week1-transformer.md` 与 `experiments/transformer_baseline.ipynb` 已创建；notebook 已使用 `machine-learning-two` kernel 从头执行，6 个代码 cell 全部成功且无错误输出。
- 第 2 周已经完成 next-token prediction、Tokenizer、上下文长度、decoder-only、causal mask、数据质量、污染、精确与近似去重、候选生成及小型金标准评测等内容。
- 预训练与普通监督学习的区别已经完成讲解并通过回答验收。
- Scaling Laws 的基本关系、固定计算预算、计算最优分配、经验曲线边界和公平实验条件已经完成讲解并通过回答验收，训练步数实验也已可复现运行。
- 已完成 one-layer decoder-only tiny language model，使用固定随机种子、模型、Tokenizer、训练步数和验证集运行两种数据处理方案。
- 已生成两种数据处理方法的训练 loss、验证 loss、训练时间和生成样例，并保存到 `outputs/tiny_lm_comparison.json`。
- 数据处理对照已扩展为五个相同随机种子的配对实验；去重方案在五次中均取得更低验证 loss，平均差值约为 `-0.1301`，但小语料与少量种子仍限制结论外推。
- 已完成固定模型与数据、改变训练步数的五种子实验：20、60、120 步的平均验证 loss 分别约为 `3.5481`、`3.0575` 和 `3.4376`，显示小数据上继续增加训练计算可能因过拟合而使验证结果变差。
- 已形成 `notes/week2-pretraining-and-data.md`，覆盖 next-token prediction、预训练与监督学习、Tokenizer、decoder-only、Scaling Laws、Data Curation、指标、实验结果和失败边界。
- 第 1 周指定路径下的两个交付物已补齐并通过 notebook 执行验证；第 2 周的知识、代码实验、结构化笔记和 next-token 滑动窗口复测均已完成，因此前两周严格验收完成。

## 后续学习路线

- 总目标不是刷完三门课程，而是形成一条可复现、可评测、可汇报的连续证据链。
- 第 1 周已完成 Transformer 核心内容、规定 Markdown 笔记和可执行 notebook 交付物。
- 第 2 周已完成预训练与数据核心知识、数据整理实验、tiny language model 对照、Scaling Laws 实验、结构化课程笔记和滑动窗口复测。
- 第 3 周学习 Princeton 的 Instruction Tuning、Learning from Preferences、DPO/PPO、Alignment 与 Constitutional AI，先完成概念、指标和小规模可复现实验。
- 第 4 周学习 Princeton 的 Reasoning、Test-Time Compute、RAG 与 Long Context，完成直接回答、普通 RAG、查询改写 RAG 三个基线及答案质量、召回率、延迟和 token 成本评测。
- 第 4 周结束后形成第一次正式阶段汇报：仓库链接、一页总结、一张结果表、三个失败案例和一个研究问题。
- 第 5—6 周选学 Berkeley 的 `LLM Agents Overview`、`Evolution of system designs` 与 `Agent Evaluation`，在已有 RAG 上加入显式状态流转和工具调用，并记录步骤、调用次数、成本和失败类型。
- 第 7 周学习 Stanford CS329A 的 `Test-time Compute Scaling` 与 `Robust Verification`，只加入一种可归因的改进机制，例如 Best-of-N、verifier 或 retry。
- 第 8 周完成直接回答、普通 RAG、Agentic RAG、带验证或重试版本的统一对照实验和 4—6 页报告。
- 第 3 周可以完成小规模 SFT 或偏好优化实验，但不要求训练大模型；完整 PPO、多智能体和图增强检索仍作为后续可选扩展。
